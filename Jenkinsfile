//TODO: config jenkinsfine to crate a pipeline to build image docker and push to docker hub

pipeline {
    stages {
        stage('Test') {
            steps {
                sh 'echo "Hello World"'
            }
        }
    }
}